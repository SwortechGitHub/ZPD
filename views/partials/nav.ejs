<nav class="navbar navbar-expand-lg navbar-light">
    <img src="/images/logo.png" alt="Logo" style="height: 50px;">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <% webPages.forEach(page => { %>
          <% if (page.parent === "none") { %>
            <li class="nav-item">
              <a class="nav-link" href="<%= page.route %>"><%= page.name %></a>
            </li>
          <% } %>
        <% }); %>
        <li class="nav-item">
          <a href="/news">Ziņas</a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Macības
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
            <% webPages.filter(childPage => childPage.parent === "Macibas").forEach(childPage => { %>
              <a class="dropdown-item" href="<%= childPage.route %>"><%= childPage.name %></a>
            <% }); %>
          </div>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Skola
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
            <% webPages.filter(childPage => childPage.parent === "Skola").forEach(childPage => { %>
              <a class="dropdown-item" href="<%= childPage.route %>"><%= childPage.name %></a>
            <% }); %>
          </div>
        </li>
      </ul>
    </div>
  </nav>